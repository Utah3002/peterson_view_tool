require "spec_helper"

describe PetersonViewTool do
  it "has a version number" do
    expect(PetersonViewTool::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
